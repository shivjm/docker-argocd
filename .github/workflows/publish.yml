name: Build and publish to ghcr.io

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md

jobs:
  build:
    uses: "shivjm/docker-argocd/.github/workflows/docker.yml@10314b218722b539f7148fa38fc63d264770777e"
    with:
      push: true
