name: Build and publish to ghcr.io

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md

jobs:
  build:
    uses: "shivjm/docker-argocd/.github/workflows/docker.yml@2227264f4b48892a966ecc067c0ef446ff3b1d8d"
    with:
      push: true
