name: Build and publish to ghcr.io

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md

jobs:
  build:
    uses: "shivjm/docker-argocd/.github/workflows/docker.yml@c5b42b8ff8073366fd6d8a4f01dbbbfc48432910"
    with:
      push: true
