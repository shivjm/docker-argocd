name: Build and publish to ghcr.io

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md

jobs:
  build:
    uses: "shivjm/docker-argocd/.github/workflows/docker.yml@8c0b2c8e528472078208715dcfd224f40611e183"
    with:
      push: true
