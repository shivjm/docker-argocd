name: Build and publish to ghcr.io

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md

jobs:
  build:
    uses: "shivjm/docker-argocd/.github/workflows/docker.yml@f9d3b4607b6b505b7a689e5e0cdddbfe05ec48fa"
    with:
      push: true
