name: Build and publish to ghcr.io

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md

jobs:
  build:
    uses: "shivjm/docker-argocd/.github/workflows/docker.yml@0f5a096d63bbe276ede5cb6fa68b54e457abe6f9"
    with:
      push: true
